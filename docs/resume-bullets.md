# Resume Bullets — AI Engineering Project

Generated by `/resume-update` command. Add to your resume after completing each phase.

---

## 2026-07-03 — Phases 1-4

- **IBM ContextForge / Federated MCP Gateway** — Architected and deployed a federated Model Context
  Protocol (MCP) AI gateway on Azure Kubernetes Service, unifying 5 heterogeneous MCP servers
  (custom Python FastMCP, GitHub, Azure DevOps, Kubernetes, Prometheus) behind a single endpoint
  and federating 86 tools with an exact-match verification pass.
  Stack: IBM ContextForge, AKS, MCP, Python (FastMCP), Bicep, Helm.

- **RBAC & Zero-Trust Access Control** — Designed and implemented team-scoped RBAC using
  gateway-native virtual servers, isolating tool access by function (86-tool `sre-full` vs.
  62-tool `dev-tools`) and diagnosing an upstream admin-bypass authorization bug through direct
  log analysis rather than assuming platform correctness.
  Stack: ContextForge RBAC, virtual servers, REST API-driven config.

- **Microsoft Entra ID / OIDC SSO Integration** — Stood up Entra ID SSO for a self-hosted AI
  gateway, resolving real-world identity edge cases (missing `mail` claims on auto-provisioned
  tenants, local-vs-SSO account collision handling) by reading gateway source directly rather
  than trusting vendor tutorial docs, and validated the full login flow end-to-end with a live
  SSE session under a non-admin, team-scoped identity.
  Stack: Microsoft Entra ID, OIDC, Azure AD App Registrations, Microsoft Graph API.

- **Azure Infrastructure as Code (Bicep)** — Authored modular Bicep templates provisioning a
  production AKS environment (VNet, ACR, Key Vault, workload identity, Container Insights) with
  autoscaling (2-10 nodes), least-privilege managed identities per workload, and idempotent
  redeploys — catching and fixing a real IaC drift incident where a stale default silently
  reverted a manual node-pool fix.
  Stack: Bicep, AKS, Azure Key Vault, Azure Container Registry, Azure Workload Identity.

- **Kubernetes / Helm Production Deployment** — Deployed and operated a multi-service Helm-managed
  stack on AKS with TLS via cert-manager/Let's Encrypt, NetworkPolicy-enforced egress controls,
  and CSI-driven secret injection from Key Vault — resolving production-grade networking issues
  including Azure Standard LB SNAT asymmetry and non-VNet-integrated API server egress.
  Stack: Kubernetes, Helm, cert-manager, NGINX Ingress, Azure Key Vault CSI driver.

- **Incident Diagnosis & Root-Cause Analysis** — Independently triaged and resolved 15+ real
  production incidents across the platform build-out (auth failures, IaC drift, malformed k8s
  manifests, wrong image tags, CrashLoopBackOff, API pagination truncation), each documented
  with root cause and fix in versioned runbooks for future reference.
  Stack: kubectl, Azure CLI, Helm, git-tracked runbooks.

